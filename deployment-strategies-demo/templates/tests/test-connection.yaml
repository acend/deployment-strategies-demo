apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deployment-strategies-demo.fullname" . }}-test-connection"
  labels:
    {{- include "deployment-strategies-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deployment-strategies-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
